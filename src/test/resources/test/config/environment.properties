# =====================================================================================================================================
# Property file which is used to inject the application url and browser in which automation needs to execute 
# =====================================================================================================================================

# The weather shopper url
weather.shopper.url : https://weathershopper.pythonanywhere.com/

# This property decides which browser will get launch while executing the automation test cases
app.browser.name : Chrome
#app.browser.name : Mozilla Firefox
